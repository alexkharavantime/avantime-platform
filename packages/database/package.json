{
  "name": "@avantime/database",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo Database lint pending",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.8.0",
    "tsx": "4.19.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts"
}

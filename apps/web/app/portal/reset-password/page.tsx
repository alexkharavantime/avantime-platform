import { PageShell } from '../../../components/page-shell';import { ResetPasswordForm } from '../../../components/portal/reset-password-form';export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=''}=await searchParams;return <PageShell><section className="bg-slate-50 py-20"><div className="mx-auto max-w-md px-6"><div className="rounded-[2rem] bg-white p-8 shadow-xl"><p className="eyebrow">Безопасность</p><h1 className="mt-4 text-4xl font-black">Новый пароль</h1>{token?<ResetPasswordForm token={token}/>:<p className="mt-6 text-red-700">Токен отсутствует.</p>}</div></div></section></PageShell>}

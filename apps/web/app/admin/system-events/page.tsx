import { redirect } from 'next/navigation';import { PageShell } from '../../../components/page-shell';import { getSession } from '../../../lib/session';import { listSystemEvents } from '../../../lib/system-events';export default async function Page(){const session=await getSession();if(!session||session.role!=='ADMIN')redirect('/portal/login');const items=await listSystemEvents();return <PageShell><section className="bg-slate-50 py-16"><div className="mx-auto max-w-6xl px-6"><p className="eyebrow">Администрирование</p><h1 className="mt-4 text-5xl font-black">Системные события</h1><div className="mt-10 overflow-hidden rounded-3xl border border-slate-200 bg-white">{items.length===0?<p className="p-6 text-slate-500">Событий пока нет.</p>:items.map(i=><div key={i.id} className="grid gap-2 border-b border-slate-100 p-5 sm:grid-cols-[120px_160px_1fr_190px]"><strong>{i.level}</strong><span>{i.category}</span><span>{i.message}{i.actorEmail?` · ${i.actorEmail}`:''}</span><time className="text-sm text-slate-500">{new Date(i.createdAt).toLocaleString('ru-RU')}</time></div>)}</div></div></section></PageShell>}

'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';
export function ResetPasswordForm({token}:{token:string}){const router=useRouter();const[password,setPassword]=useState('');const[message,setMessage]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json() as {message?:string;error?:string};setMessage(d.error??d.message??'');if(r.ok)setTimeout(()=>router.push('/portal/login'),800)}return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-sm font-bold">Новый пароль</span><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-2xl border border-slate-200 px-4 py-3"/></label><button className="w-full rounded-full bg-blue-600 px-5 py-3 font-black text-white">Изменить пароль</button>{message&&<p className="rounded-2xl bg-slate-50 p-4 text-sm">{message}</p>}</form>}

'use client';
import { useState } from 'react';
export function ForgotPasswordForm(){const[email,setEmail]=useState('');const[result,setResult]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json() as {message?:string;error?:string;resetUrl?:string};setResult(d.error??`${d.message}${d.resetUrl?` Демонстрационная ссылка: ${d.resetUrl}`:''}`)}return <form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-sm font-bold">Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-2xl border border-slate-200 px-4 py-3"/></label><button className="w-full rounded-full bg-blue-600 px-5 py-3 font-black text-white">Получить ссылку</button>{result&&<p className="rounded-2xl bg-slate-50 p-4 text-sm leading-6">{result}</p>}</form>}
